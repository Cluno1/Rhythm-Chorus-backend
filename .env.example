RHYTHM_ENVIRONMENT=development
RHYTHM_BOOTSTRAP_TOKEN=replace-with-a-long-random-token
RHYTHM_DATABASE_PATH=./var/rhythm.sqlite3
RHYTHM_V2_DATABASE_PATH=./var/rhythm-v2.sqlite3
RHYTHM_LOCAL_OBJECT_ROOT=./var/objects

# Public gateway only. Generate both values locally and never commit the result.
# python -c 'import secrets; print(secrets.token_urlsafe(48))'
RHYTHM_PUBLIC_TOKEN_SECRET=
RHYTHM_PUBLIC_ADMIN_USERNAME=admin
# python -c 'from rhythm_metadata_api.application.device_auth import hash_admin_password; print(hash_admin_password(input("Admin password: ")))'
RHYTHM_PUBLIC_ADMIN_PASSWORD_HASH=
# Seven-day absolute lifetime for the Web administrator session token.
RHYTHM_PUBLIC_ADMIN_TOKEN_TTL_SECONDS=604800
RHYTHM_PUBLIC_INVITE_TTL_SECONDS=600

# Sonorus self-hosted updates. Keep the host directory writable only through
# the deployment account; public-api mounts it read-only.
RHYTHM_SONORUS_UPDATES_ROOT=./updates
RHYTHM_SONORUS_DEBUG_CERTIFICATE_SHA256=
RHYTHM_SONORUS_STABLE_CERTIFICATE_SHA256=

# Issue 78 online chorus. Production device uploads and mixed playback require
# one private COS bucket; the API only returns short-lived HTTPS signed URLs.
RHYTHM_CHORUS_COS_BUCKET=
RHYTHM_COS_REGION=ap-guangzhou
RHYTHM_COS_SECRET_ID=
RHYTHM_COS_SECRET_KEY=
RHYTHM_COS_PRESIGN_EXPIRES_SECONDS=900
RHYTHM_CHORUS_FFMPEG_PATH=ffmpeg
RHYTHM_CHORUS_MIX_TIMEOUT_SECONDS=300

# Issue 91 Labs images. Keep the bucket private. The preview host is a bare
# hostname (no scheme/path); use an HTTPS custom COS origin for browser preview.
# The COS default host can be used for native-client rollout while DNS and the
# custom HTTPS origin are being prepared.
RHYTHM_CLIENT_IMAGE_COS_BUCKET=
RHYTHM_CLIENT_IMAGE_PREVIEW_HOST=
RHYTHM_CLIENT_IMAGE_CI_ENABLED=false
RHYTHM_CLIENT_IMAGE_PRESIGN_EXPIRES_SECONDS=600
RHYTHM_CLIENT_IMAGE_SHARED_EXPIRES_SECONDS=60
RHYTHM_CLIENT_IMAGE_MAX_BYTES=20971520
RHYTHM_CLIENT_IMAGE_MAX_PIXELS=40000000
RHYTHM_CLIENT_IMAGE_MAX_BATCH_ITEMS=500
RHYTHM_CLIENT_IMAGE_MAX_THUMBNAIL_DELIVERIES=30
RHYTHM_CLIENT_IMAGE_MAX_PARALLEL_UPLOADS=3
RHYTHM_CLIENT_IMAGE_STORAGE_QUOTA_BYTES=5368709120
